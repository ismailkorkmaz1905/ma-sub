name: deterministic-repair-release-v0.1.0

on:
  push:
    branches: [main]
    paths: ['.github/workflows/repair-release-v010c.yml']

permissions:
  contents: write

jobs:
  repair-test-tag:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Repair only an incomplete production wrapper
        run: python tools/repair_v010.py
      - name: Remove obsolete bootstrap and one-shot files before testing
        shell: bash
        run: |
          set -euo pipefail
          rm -rf bootstrap
          rm -f .github/workflows/bootstrap.yml
          chmod +x mas runpod/bootstrap.sh runpod/preflight.sh
      - name: Compile and run complete CPU regression suite
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install PyYAML==6.0.2 openpyxl==3.1.5 pytest==8.3.5
          export PYTHONPATH="$PWD/src${PYTHONPATH:+:$PYTHONPATH}"
          python -m compileall -q src tests
          ./mas --help >/dev/null
          ./mas test | tee /tmp/pytest.txt
      - name: Docker smoke build
        shell: bash
        run: |
          set -euo pipefail
          if grep -Eq '^FROM .* AS test$' Dockerfile; then
            docker build --target test -t ma-sub:v0.1.0-test .
          else
            docker build -t ma-sub:v0.1.0-test .
          fi
      - name: Commit the exact tested tree, clean one-shot files, and tag
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'ma-sub release gate'
          git config user.email 'actions@users.noreply.github.com'
          rm -f tools/repair_v010.py
          rm -f .github/workflows/repair-release-v010.yml
          rm -f .github/workflows/repair-release-v010c.yml
          rm -f .github/workflows/release-v0.1.0.yml
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m 'Finalize tested checkpointed pipeline v0.1.0'
          fi
          git push origin HEAD:main
          if git rev-parse -q --verify refs/tags/v0.1.0 >/dev/null; then
            if test "$(git rev-parse 'v0.1.0^{tree}')" != "$(git rev-parse 'HEAD^{tree}')"; then
              echo 'Existing v0.1.0 tag does not match the tested production tree.' >&2
              exit 1
            fi
          else
            git tag -a v0.1.0 -m 'ma-sub v0.1.0'
            git push origin refs/tags/v0.1.0
          fi
          test -z "$(git status --porcelain)"
          echo "main_commit=$(git rev-parse HEAD)" >> "$GITHUB_STEP_SUMMARY"
          echo "tag_commit=$(git rev-parse 'v0.1.0^{}')" >> "$GITHUB_STEP_SUMMARY"
          tail -n 1 /tmp/pytest.txt >> "$GITHUB_STEP_SUMMARY"
