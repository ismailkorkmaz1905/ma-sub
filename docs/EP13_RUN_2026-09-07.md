# Episode 13 preflight, 2026-09-07 Singapore

Episode 13 is BLOCKED_SOURCE_URL. No verified official full-episode URL exists
in the inspected maintained records or local Episode 13 artifacts. The operator
was asked for that missing input. No download, paid Pod, controller, inference,
long local encode or Drive publication was started.

Initial HEAD: `2a8c8fe1a6612e32db5ee8f998fa359da9397d70`, clean `main`.
Episode 12 artifacts remain in the external archive, unchanged. No source hash
was changed. Local evidence: `var/ep13-preflight-20260907/` (ignored and retained).

## Eleven stages

| Stage | Actual status |
|---|---|
| download | BLOCKED: official Episode 13 source URL requested. |
| audio | NOT_STARTED |
| raw_asr | NOT_STARTED |
| tr_pack | NOT_STARTED |
| tr_return | NOT_STARTED |
| audio_review | NOT_STARTED |
| forced_alignment | NOT_STARTED |
| id_pack | NOT_STARTED |
| id_return | NOT_STARTED |
| finalize | NOT_STARTED |
| drive_readback | NOT_STARTED |

`mas status 13` returned empty stages; its display timestamp does not establish
a run. Evidence: `status13.log`, `preflight-summary.json` and
`local-production-processes.json`. Perceptual listening was NOT_PERFORMED.

## External RunPod evidence

`provider-inventory.json`, observed 2026-09-06 17:12:38 UTC (September 7,
01:12:38 Singapore), records:

| Field | Observed value |
|---|---|
| Account balance | 2.8569866595 USD |
| Account spend | 0.005 USD/hour |
| Auto-pay | false |
| Only Pod | `781ct55zv4gkle`, EXITED |
| Preserved volume | `xgogcmey5o`, 50 GB, EU-RO-1 |
| Actual occupied volume bytes | UNKNOWN; API reports allocation only |
| Newly owned Pods | None |

The old Pod's 0.74 USD/hour rate is not active GPU billing. Reserving 1.00 USD
plus 0.10 USD shutdown/billing margin leaves 1.7569866595 USD before future
storage charges. This subtraction is not a spend or runtime guarantee.

`gpu-prices.json`: live EU-RO-1 RTX 4000 Ada 20 GB quote is 0.28 USD/hour,
Low stock. L4 24 GB price and stock are null; no current L4 quote is established.
No allocation was attempted.

Current official [pricing](https://docs.runpod.io/pods/pricing) lists standard
network storage at 0.07 USD/GB/month below 1 TB: 50 GB x 0.07 = 3.50 USD/month.
Running container storage is 0.10 USD/GB/month. Re-query before spending.
Actual quota usage and scratch capacity remain unverified; `df` is insufficient.

## Code, tests and notifications

Commit `8f2891b` fixes SMTP data rejection handling. Previously Gmail 550 daily
quota errors reached the three-attempt retry branch. Permanent SMTP responses
in that branch now return `blocked` without retry. Temporary 451 errors retain
bounded retries. Authentication/recipient rejection semantics are unchanged.

| Post-fix local check | Result |
|---|---|
| Notification tests | 10 passed in 0.21 seconds |
| Full suite | 744 passed, 5 skipped in 59.53 seconds |
| git diff --check | PASS |
| doctor | Python/FFmpeg/FFprobe/Git/rclone OK; torch MISSING locally |

Evidence: `post-fix-notify-tests.log`, `post-fix-full-tests.log` and their status
JSON files. These are local tests, not CI, real GPU, Drive or quality acceptance.
No CPU ASR fallback occurred. Dockerfile and RunPod bootstrap/preflight/watchdog/
stop scripts were inspected; no new Docker build was performed.

One configured Gmail preflight/status email was SMTP-accepted in 3.687 seconds.
`notification.json` retains timestamps and message identity; inbox receipt is
not proven. No quota retry loop occurred.

One helper performed preparation and tests. Sol was requested, but the helper
reported only generic GPT-5 identity; exact deployed model is UNKNOWN. No new
comprehensive review was opened.

## Remaining prerequisites

- Receive and metadata-verify the official full Episode 13 URL and title.
- Refresh balance, GPU price, cookies, Drive access and sole-controller inventory.
- Establish the uniquely owned temporary-Pod route for `.\mas.ps1 run 13`
  (Linux `./mas run 13`); do not blindly start the default retained Pod. Preserve
  checkpoints and externally verify deletion of only newly owned Pods.
- Check actual network-volume usage, cache cleanup safety and container scratch
  capacity. Complete short SSH/hash commands and a real remote MP4 sample first.
- Current encoder is H.264 NVENC p4, VBR, CQ 19, `-b:v 0`, AAC 192 kbit/second.
  Approximate 3 GB targeting is still unimplemented. Select bitrate/codec using
  episode duration and measured samples; do not alter Episode 12 MP4.
- SSH already uses `-n -T`; the historical hang's cause remains UNKNOWN.
- Execute all strict stages, exact TR/ID returns and acoustic gates. Stop GPU
  during translation/review/Drive waits. Preserve strict MKV/SRT QA before the
  separate MP4 receipt and collision-preserving Drive byte/SHA-256 readback.

No transport or quality PASS, top-up, transcription API, volume deletion, stable
tag or Episode 14 run occurred. The authorized Episode 13 task remains incomplete.
